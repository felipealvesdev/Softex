const altura = parseFloat(prompt("Digite a altura(cm) do triângulo:"));
const base = parseFloat(prompt("Digite a base(cm) do triângulo:"));

const area = (altura*base) / 2;

alert(`A área do triângulo informado é de: ${area}cm²`);
